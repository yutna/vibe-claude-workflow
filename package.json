{
  "name": "claude-workflow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test --test-name-pattern=. test/workflow_hook.test.cjs",
    "test:watch": "node --test --watch test/workflow_hook.test.cjs"
  }
}
